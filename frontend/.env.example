# ============================================
# FRONTEND · Variables de entorno
# ============================================
# Copia este archivo a `.env` (o `.env.local`) y ajusta los valores.

# URL del backend Express. Debe coincidir con el PORT del backend.
NEXT_PUBLIC_API_URL=http://localhost:3001
